package AlgorithmicComplexity;

public class AlgorithmicComplexity {
	
	
	public static void main(String args[]) {
		
	}
	
	/**
	 * O(1)
	 */
	
	/**
	 * O(N)
	 * Example: Linear search. Worst case scenario is that the whole array must be searched
	 */
	public void linearSearchArray(int value) {
		
	}
	
	/**
	 * O(N^2)
	 * Example: Bubble sort. Each pass of the outer requires the inner loop to iterate through the array.
	 */
	
	/**
	 * O(N^3)
	 */
	
	/**
	 * O(log(N))
	 * Example: Binary search. Each pass through the algorithm the data by about 50 %.
	 */
	
	/**
	 * O(n log(n))
	 * Example Quick sort. The final sorted list is reduced in half.
	 */
	

}
